/// Application-level metadata and UI constants.
abstract class AppMetadata {
  /// User-visible application title.
  static const String title = 'Make It Sound Natural';

  /// Top inset needed when using the transparent macOS titlebar.
  static const double macOSTitlebarTopPadding = 28;
}
